#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../blue-tape/bin/blue-tape.js" "$@"
  ret=$?
else 
  node  "$basedir/../blue-tape/bin/blue-tape.js" "$@"
  ret=$?
fi
exit $ret
